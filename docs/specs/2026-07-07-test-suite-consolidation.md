# Test-suite consolidation — remove low-quality tests, one owner per behavior

**Status: executed (2026-07-08, branch `testing-refactor`, 19 commits from `478d691`; outcome + deviations at the bottom).** The execution plan distilled from this session's four-way audit of `tests/` (1472 tests / 56 files, all green, 5.7s). The suite's discipline is clean — zero own-module mocking, seams-only fakes, real fs/git — so this is not a rescue: it removes the two concentrated debt classes (prose-brittleness outside the parity harness, duplicated coverage without a single owner), then closes the handful of real gaps the audit exposed. Analysis details live in the session record; this file is the working checklist.

## The bar

A high-quality test measures intended behavior through a public interface and survives internal refactors. A low-quality test is trivial, restates the implementation, or breaks on internal factors (a wording improvement, a rail rename) when behavior didn't change. We remove or refactor low-quality tests rather than keep them for coverage; coverage that costs churn is debt.

Governing rules for every step:

- **Byte-level prose belongs in `tests/parity/`** (model-facing) or an explicit schema/contract pin. Elsewhere, keep the behavioral nucleus: presence/absence flips (`.not.toContain`), relational equality between two renders (`toBe` — wording-independent), recovery-token `toContain`s (a refusal must name the next move), `--json` schema key-sets, acting-command strings.
- **Wording-is-contract survives**: the health taxonomy's error-string table (matches real CLI prose), CLI refusals that name the next command, the `status --json` schema, skill↔CLI coherence scans, the snippet-library contract core.
- **One owner per behavior.** A second file may keep only its *delta* (a transport, a CLI framing, a wiring), never a re-proof.
- **No `src/**` changes in any step.** Every step is tests/ (plus docs in E1). If loosening or moving a test turns it red, that is a finding to surface, not to patch.
- **Parity pins must not move**: `git status tests/parity/pins` stays clean through the whole plan (structurally guaranteed by the no-src rule — verify anyway at E2). Wording improvements to prompts/status output themselves are explicitly deferred; this plan only removes the friction that makes them expensive later.
- One step = one commit (A2 and B1 may split into two), suite green after each.

## Phase A — preparatory extractions (structure only, zero semantic change)

- [x] **A1. `tests/helpers/replay.ts`.** Hoist the copy-pasted `stamp`/`entry` (canonical: `replay-record.test.ts:4-6`, the one with the `ms` param), `tool`/`toolText`, `traceWith`, `snapshotTree` (`replay-run.test.ts:146-162`), a `withReplayOutDir` tmpdir fixture, and a provider-store-sentinel helper (the manual `HOME`/`CLAUDE_CONFIG_DIR` save-restore in `replay-run.test.ts:58-142`). Update all 7 `replay-*.test.ts` files. Done when: no local `stamp`/`entry` duplicates remain.
- [x] **A2. Extract `tests/position.test.ts`** from `lifecycle.test.ts:325-597` (both `probeRunPosition` describes, ~22 tests) — `run/position.ts` finally gets its own file. First commit: pure move. Second commit: lighten the headless cases that re-drive `driveToQuiescence` only to park a snapshot (`:335,:363,:378,:393,:410,:441` → `saveMachineSnapshot` directly, the interactive half's pattern) — but keep the deliberate integration drives as-is (e.g. the `:357-369` "actual hydration path" test says so in its comment).
- [x] **A3. Split `providers.test.ts` (1321 lines, 5 seams) by seam**: `claude-provider.test.ts` (parseClaudeTurn, claudeArgs, claudeExecaOptions, ClaudeWorker.runTurn, recoverClaudeFailure), `interactive-claude.test.ts` (`:227-620`), `codex-provider.test.ts` (codexThreadOptions, CodexWorker, reconstructCodexTurn, recoverCodexAbort, parseRolloutContext), `provider-factory.test.ts` (createWorkers, providerFor). Fold the S3 wall-clock wiring block (`:1271-1321`) into `wall-clock.test.ts` beside the guard it proves. Pure move.
- [x] **A4. Fixtures + vocabulary hygiene.** Add `relayRun`/`relayConsultantRun` fixtures to `fixtures.ts` (the shipped workflow with no fixture; adopt where tests hand-roll relay runs). Fix retired "arc"/"rir"/"design arc" vocabulary in test comments: `fixtures.ts:129-134`, `scripted-machine.ts:13`, `parity/briefs.test.ts` section headers, `parity/snippets.test.ts:92`, `orchestrator-prompts.test.ts:39`.

## Phase B — consolidations (one owner per behavior; net deletions)

- [x] **B1. `tools.test.ts` structural pass** (~199 → ~160 tests; may split into two commits):
  - Rails block (`:190-360`) 14 → ~5: keep only what handlers can't reach — the `firstRefusal` ordering invariant, the async-host-only phase-exit gate (`:236-242`, a named engineering.md invariant), the orphan clear-side-effect discipline (`:199-211`). Delete the handler-shadowed cases (sameRoleInFlight, reviewCap, warnOnce, terminalAlreadySet simple cases).
  - Collapse `result builders` (`:148-167`) to one test or delete (typecheck is the real guarantee, per its own comment). Drop the `COMPACT_TIMEOUT_MS` constant restatement (`:174`).
  - `test.for` tables: salvage ladder (`:821-926`, 6→2), heartbeat-vs-live-activity mirrored cases (`:1431/:1631`, `:1452/:1647`, `:1306/:1561`, ~5 fold), consultant contract/orphan rail permutations (`:2444-2584`, ~10→4).
- [x] **B2. Host-loop dedupe.** `host-runner.test.ts` is the owner of entry-replay/nudge-once/twice-ended/crash→flag. Collapse `driver.test.ts:287-421` (~8 tests) and `stdio-host.test.ts:132-198` (~3 tests) to thin "this host wires into the shared loop" smokes. Keep driver's SDK-specific clusters (terminal-decision-survives-stream-throw, auto-retry ledger, modelUsage capture, classifyInfraError) and stdio's transport-unique cases (kill-peer mid-turn, park-across-boundary, steer-suppression, pre-auth cross).
- [x] **B3. `run-store.test.ts` merges.** Fold the "workflow identity" describe (`:600-680`) into "run creation" parameterized by workflow; convert the gatesAt (`:245-268`,`:504-522`) / retryInfra (`:280-292`) / budget (`:294-307`) matrices to `test.for`; loosen the two exact `displayName` pins (`:348,:356`) to the `:623` `toMatch` style.
- [x] **B4. Workflow-compile pin, one home.** Delete `workflow-definition.test.ts` — its byte-identical SDK-rebuild pin (`:45-51`, with an inline re-declaration of all four workflows that will rot) is subsumed by `skill.test.ts:270-284` (the stronger executable-docs version), and its plan-fixer reject by `workflow-source.test.ts:247-261` (which also asserts path-wrapping). Thin `workflows.test.ts:170-199` to the CLI-framing delta only.
- [x] **B5. Subject moves.** Move `abandon.test.ts:225-255` (a status describe) into `status.test.ts` as two more stop kinds; delete `lifecycle.test.ts:679-694` (`interactiveContinueAction`/`validateInteractiveCrossing` predicate dupes — `continue-planner.test.ts` owns the decision); thin `framing.test.ts:487-525` to "bind.* keys peel onto `meta.binds`" (the value-validation error prose is `config.test.ts`'s contract).
- [x] **B6. `decision-points` ownership.** Thin `grade.test.ts:109-140` and `stats.test.ts:334-362` to wiring assertions (a grades section renders; the walkthrough presents points) — the discovery/matrix math stays pinned only in `decision-points.test.ts`.
- [x] **B7. Graph model/render dedupe.** Fold `graph.test.ts:23-81`'s structural-facts-via-ANSI-substrings into `graph-model.test.ts` (which already owns them on the model); `graph.test.ts` keeps the `--json` schema pins, the Mermaid id-safety tests, and one render smoke. Same recipe for `graph-run.test.ts:170-179`'s glyph pins.
- [x] **B8. Provider/wall-clock dedupe + straight removals** (post-A3): drop the `claudeArgs`/`codexThreadOptions` rows re-proven byte-for-byte through `runTurn`/`createWorkers` (keep only integration-unreachable rows, e.g. budget omission); introduce the parameterized provider-contract table over `WorkerProvider` for the claude/codex mirrors (wall-clock wiring, accepted-abort recovery, session-id announcement — currently proven at three layers); merge `wall-clock.test.ts`'s two deadline suites over a `{makeGuard, trip}` table. Delete the trivials: `wall-clock.test.ts:191-193` (constant restatement), `:199-202` (FakeWorker self-test — move its comment to the S5 use site), the `providers` construction check (`:1148-1155` pre-split), `orchestrate.test.ts:211` (default-field check shadowed by `:196`), `colorize.test.ts:9-28` (near-tautological first describe).

## Phase C — wording-pin loosening (keep the behavioral nucleus, drop verbatim sentences)

- [x] **C1. `status.test.ts`** (`:27-99`, `:448-625`, ~20 tests): loosen full-sentence pins to structural tokens (section-appears-at-this-stop, the acting `duet continue …` commands, counts). Keep untouched: the `--json` schema key-set pin, stop discrimination, context-percent math, raw-UTC-vs-local boundary asserts.
- [x] **C2. Brief-fragment pins parity already owns.** `tools.test.ts:2109-2443` (~21 tests), `orchestrator-prompts.test.ts` (all four describes), `driver.test.ts:96-137`: drop positive `toContain` fragments; keep the relational `toBe`-between-renders invariance (gateless ≡ attended), presence/absence flips, `indexOf` ordering invariants, and the send-enum visibility behavior.
- [x] **C3. Scattered small pins.** `claude-provider` (ex-`providers:110`) full-message `toThrow` → subtype+cause regexes; `wall-clock` message substrings (`:188`, `:282-283`) → `.kind` + cap-value; `preflight.test.ts:35` probe-prompt text → any-non-empty-prompt; `doctor.test.ts:152,164` `'network:'`/glyph literals → localization-boundary asserts only; `abandon` `'merge the PR on GitHub'`, `grade` polarity-question prose, `stats` `'TP 0 · FP 1 …'` table string → structural presence. `snippets.test.ts` body-prose (`:137-161`, `:180-181`, `:317-320`): keep the cross-snippet section-presence property (a real library invariant parity's byte pins don't express), drop the phrase-level regexes inside sections.
- [x] **C4. `skill.test.ts` doc-lock trim.** Drop the `docs/engineering.md` prose lock (`:532-545` — a live internal doc is not a shipped surface; docs discipline belongs to the update-docs cadence). Keep the shipped-surface half (`:500-524`, help text + concierge reference) and everything else. Note: the required `'design'` token at `:414-420` is *correct* (blueprint/relay's default attended gate), not stale — leave it.

## Phase D — gap additions (new tests; each lands in its post-A3 home)

- [x] **D1. Context-deadline wired into `runTurn`** — the top gap. A "deleting the wrap would leave the suite green" guard (the style the wall-clock wiring already has) proving both providers route through `runWithContextDeadline` with the transcript sampler: fill crosses the limit mid-turn ⇒ turn cut + resumable checkpoint. In `claude-provider.test.ts` + `codex-provider.test.ts`.
- [x] **D2. Codex resume + context-exhaustion.** A resume turn drives `resumeThread(id)` (not `startThread`), announces the id immediately, captures resume options; and a codex turn cut by the context deadline after `thread.started` recovers as an aborted+context-exhausted checkpoint (the claude variant exists; codex's doesn't).
- [x] **D3. Doctor positive verdicts.** Produce and render `retrying`, `long-inference`, and `silent-stuck` (three of six verdicts currently only negatively asserted) — transcript fixtures via `tests/helpers/transcripts.ts`.
- [x] **D4. Corpus + ledger.** `reconcileRecord` (the full re-sync incl. the destructive `steers/` replace — and its fail-soft contract: a corpus failure never affects the run) and `transcriptArchiveDir`; the context-event ledger companions `sampleContextUsage`/`recordContextEvent`/`contextEventReading` (`store.ts:1001-1022`).
- [x] **D5. Small closures.** `killDriver` SIGTERM→SIGKILL escalation (a signal-ignoring child); `create_branch` success asserted against real git state in the tmpdir (git-init the fixture), not the result string; `opensPr`/`phaseLoopOf` units in the new `position.test.ts`; high-water-vs-last-reading sampler wiring; `write_note` interleaved cross-role appends. Optional if cheap: interactive abort-then-drain ordering at the integration level.

## Phase E — docs + wrap-up

- [x] **E1. Docs sync** (per `docs/documentation-standards.md`): `docs/engineering.md` §Testing strategy — precision on "view glue deliberately untested" (tmux/notify stay untested; colorize's activity-line promotion and timefmt are tested oracles by design), note `position.test.ts`, the replay helper, the provider-file split, and the dropped engineering.md prose lock.
- [x] **E2. Final verification.** Full suite + `pnpm typecheck`; `git status tests/parity/pins` clean; before/after counts recorded here.

## Expected outcome

~150–200 tests removed or collapsed (tools.test.ts ~199→~160, providers split and thinned, lifecycle ~-25) with zero behavioral guarantees lost; the "breaks on a pure wording improvement" class reduced from ~90–105 tests to parity-only by design; ~6 targeted additions closing the real gaps (context-deadline wiring, codex resume, doctor verdicts, corpus reconcile, escalation paths). The suite stays the same shape everywhere else: behavior through real interfaces, fakes only at seams, fs/git real.

## Outcome (E2, 2026-07-08)

**1472 tests / 56 files → 1464 tests / 60 files, all green (6.0s); `pnpm typecheck` clean; `src/**` byte-identical to the branch point across all 19 commits; parity pins vs the merge-base show exactly the two deliberate additions.** The near-flat raw count hides the composition change: ~60 wording-coupled, trivial, or duplicated tests were removed or collapsed into tables, offset by ~50 additions — the D-phase gap tests (the mutation-verified context-deadline wiring guard, codex resume, three doctor verdicts, corpus reconcile + the context-event ledger, killDriver escalation) plus table rows and two new parity pins. tools.test.ts 200→193 but with the rails block 14→8 handler-unreachable cases and three matrices tabled; the wording-breakage class outside parity/contract pins is ~0 by construction (verified: the C-phase loosenings kept every test green with no src change).

**Two parity-matrix gaps found and closed** (fragment-dropping never lost coverage): `full-spec.draft-consultant` (the draft-entry + consultant spec world) and `full-implement.degraded` (the cross-provider builder edge).

**Deviations from the plan as written — each verified, not skipped silently:**
- A1: `traceWith` stayed local to `replay-diff.test.ts` (single consumer; pass-through helpers get deleted on sight).
- B1: the heartbeat/live-activity "mirrors" were not folded — they assert two different output contracts, not one behavior twice.
- B4: the blueprint and short SDK-rebuild pins moved to `workflows.test.ts` instead of dropping — `skill.test.ts`'s executable fences cover only relay + full.
- B5: the lifecycle predicate pair (`interactiveContinueAction`/`validateInteractiveCrossing`) was **kept** — verification found four branches with no `continue-planner.test.ts` twin (reject-at-handoff, headless-with-decision, answer-at-a-gate, decision-at-a-rest); a comment there records the condition for a future delete.
- B8: the claude/codex provider-contract table was skipped — asymmetric setup (transcript-tail plumbing vs `(error, threadId)`) meant no code reduction.
- C3: `snippets.test.ts`'s flagged body-prose tests were left intact — on close read they already follow one-token-per-fact discipline with documented rationale.
- D1: codex is **out of context-deadline scope by design** (auto-compacts; `contextCapTokens` ignored) — pinned with a negative guard and a documenting `recoverCodexAbort` re-throw test instead of a forced checkpoint.
- D5 non-gaps (audit claims that dissolved under verification): `create_branch` already asserts real git state; `write_note`'s quiescence exemption is already tested and interleaved appends are an OS boundary; `opensPr`/`phaseLoopOf` are covered through the probe/completion surfaces; the interactive abort-then-drain integration re-proof was skipped (the pure-level race guard + provider settle tests carry it).

**Merge note:** main moved ahead (#39–#41) after this branch's base; #40 changed finish/design brief prose and its pins. On rebase/merge, the 10 main-side pin updates apply cleanly (this branch never touched them), and the two NEW pins should be re-verified — if #40's prose reaches the spec/implement briefs, the file snapshots will go red and want a deliberate `-u` regeneration for exactly those two files.
